// Copy this file to openaiConfig.ts and fill in your key.
// openaiConfig.ts is gitignored — never commit your real key.
// Get a key at: https://platform.openai.com/api-keys

export const OPENAI_API_KEY = '';

export const TTS_VOICE = 'nova'; // alloy | echo | fable | onyx | nova | shimmer
export const TTS_MODEL = 'tts-1'; // tts-1 (faster) | tts-1-hd (higher quality)
